 <ul class="nav nav-tabs" id="myTab" role="tablist">
   <li class="nav-item">
     <a class="nav-link active text-secondary" id="home-tab" data-toggle="tab" href="#mybooks" role="tab" aria-controls="home" aria-selected="true">My books</a>
   </li>
   <li class="nav-item">
     <a class="nav-link text-secondary" id="profile-tab" data-toggle="tab" href="#mybookings" role="tab" aria-controls="profile" aria-selected="false">My bookings</a>
   </li>
   <li class="nav-item">
     <a class="nav-link text-secondary" id="contact-tab" data-toggle="tab" href="#contact" role="tab" aria-controls="contact" aria-selected="false">My informations</a>
   </li>
 </ul>
 <div class="tab-content" id="myTabContent">
   <div class="tab-pane fade show active" id="mybooks" role="tabpanel" aria-labelledby="home-tab">
    <div class="container mx-4 mt-4">
      <div class="row">
        <% @books.each do |book| %>
          <div class="col-3">
            <div class="card-style">
              <div class="card-img-thumbnail">
                <% if book.photo.attached? %>
                  <%= cl_image_tag book.photo.key, height: 250, width: 200 %>
                <% else %>
                  <%= image_tag book.cover_url, height: 250, width: 200 %>
                <% end %>
              </div>
              <div class="p-2 mt-4">
                <%= link_to book_path(book) do %>
                  <h1><%= book.title %></h1>
                  <div>
                    <%# book.rating.times do %>
                      <span>⭐️</span>
                    <%# end %>
                  </div>
                <% end %>
              </div>
            </div>
          </div>
        <% end %>
      </div>
    </div>
   </div>
   <div class="tab-pane fade" id="mybookings" role="tabpanel" aria-labelledby="profile-tab">
    <div class="container mx-4 mt-4">
      <div class="row">
        <% if @bookings.empty? %>
        <p>
          You don't have any reservation
        </p>
        <% else %>
          <% @bookings.each do |booking| %>
          <div class="col-3">
            <div class="card-style">
              <%= link_to book_path(booking.book) do %>
                <div class="card-img-thumbnail" style='background-image: url("<%= cl_image_path(booking.book.photo.key, height: 300, width: 250, crop: :fill) %>")'>
                </div>
              <% end %>
              <div class="p-2 mt-4">
                <%= link_to book_path(booking.book) do %>
                  <h1><%= booking.book.title %></h1>
                  <p class="text-secondary mb-0"> <strong>Begin date:</strong> <%= booking.begin_date.strftime("%e %B %Y") %></p>
                  <p class="text-secondary"> <strong>End date:</strong> <%= booking.end_date.strftime("%e %B %Y") %></p>
                <% end %>
                <% end %>
              </div>
            </div>
          </div>
        <% end %>
      </div>
    </div>
     <!-- <div class="container mt-4">
       <div class="row">
         <div class="col-6">
            <% if @bookings.empty? %>
              <p>
                You don't have any reservation
              </p>
            <% else %>
             <% @bookings.each do |booking| %>
           <div class="book-show-card">
               <p><%= booking.book.title %></p>
               <p><%= booking.begin_date %></p>
               <p><%= booking.end_date %></p>
           </div>
             <% end %>
             <% end %>
         </div>
       </div>
     </div> -->
   </div>

   <div class="tab-pane fade" id="contact" role="tabpanel" aria-labelledby="contact-tab">
     <div class="container mt-4">
       <div class="book-show-card">
         <p><strong>Name:</strong> <%= @user.first_name + " " + @user.last_name %></p>
         <p><strong>Email address:</strong> <%= @user.email %></p>
         <p><strong>Address:</strong> <%= @user.address %></p>
         <p><strong>Your rating:</strong> <%= @user.address %></p>
         <%= link_to "Change my informations", edit_user_registration_path%>
       </div>
     </div>
   </div>
 </div>
